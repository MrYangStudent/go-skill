# 完整开发工作流

## 增量开发工作流

```
┌─────────────────────────────────────────────────────────────────┐
│                      阶段零：边界确认                            │
├─────────────────────────────────────────────────────────────────┤
│  1. 明确新功能范围                                               │
│  2. 确定影响边界（哪些旧代码可能被触及）                          │
│  3. 确认向后兼容要求                                             │
│  4. 制定"不改动"策略（如必须改动，说明原因）                      │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      阶段一：需求分析                             │
├─────────────────────────────────────────────────────────────────┤
│  - 功能目的与价值                                                 │
│  - 输入→处理→输出                                                 │
│  - 约束条件（性能、安全、兼容性）                                 │
│  - 验收标准                                                       │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      阶段二：模块设计                            │
├─────────────────────────────────────────────────────────────────┤
│  1. 新模块独立目录结构                                            │
│  2. 接口设计（面向接口编程）                                      │
│  3. 依赖注入（避免硬依赖旧模块）                                   │
│  4. 向后兼容接口                                                  │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      阶段三：代码实现                             │
├─────────────────────────────────────────────────────────────────┤
│  - GoDoc 完整注释                                                 │
│  - 错误处理规范                                                   │
│  - Context 正确传递                                              │
│  - 并发安全（如涉及）                                             │
│  - 工具函数封装                                                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      阶段四：测试生成                             │
├─────────────────────────────────────────────────────────────────┤
│  - 正常路径测试                                                   │
│  - 边界情况测试                                                   │
│  - 错误路径测试                                                   │
│  - Mock 编写（如依赖外部服务）                                    │
│  - 并发安全测试（如涉及并发）                                     │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      阶段五：质量审查                             │
├─────────────────────────────────────────────────────────────────┤
│  只审查新代码文件：                                               │
│  - 错误处理审查                                                   │
│  - 并发安全审查                                                   │
│  - 安全漏洞审查                                                   │
│  - 性能审查                                                       │
│  - 日志规范审查                                                   │
│  - API 设计审查                                                   │
│  - Context 链路审查                                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      阶段六：集成验证                             │
├─────────────────────────────────────────────────────────────────┤
│  1. 编译检查：go build ./new_module                              │
│  2. 静态分析：go vet ./new_module                                │
│  3. 格式化检查：gofmt -l ./new_module                            │
│  4. 单元测试：go test -race ./new_module                        │
│  5. 覆盖率：go test -cover ./new_module                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      阶段七：文档沉淀                             │
├─────────────────────────────────────────────────────────────────┤
│  - 更新模块 README                                                │
│  - 添加 GoDoc 注释                                               │
│  - 记录设计决策                                                  │
│  - 更新 CHANGELOG                                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 新旧代码隔离检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 新功能是否有独立目录/包 | □ | 便于单独测试和审查 |
| 是否通过接口与旧代码交互 | □ | 降低耦合 |
| 是否需要修改已有接口 | □ | 如需修改，列出所有影响点 |
| 新功能是否有向后兼容要求 | □ | API/数据格式兼容 |
| 是否需要版本迁移逻辑 | □ | 数据库、配置等迁移 |

---

## 快速启动命令

```bash
# 1. 创建新模块目录
mkdir -p internal/features/<feature_name>

# 2. 初始化 Go 模块（如需）
go mod init your/module

# 3. 新功能代码结构示例
internal/features/
├── myfeature/
│   ├── myfeature.go       # 主逻辑
│   ├── myfeature_test.go   # 测试
│   ├── mock_*.go          # Mock（如需要）
│   └── doc.go             # 包级文档

# 4. 编译测试
go build ./internal/features/myfeature
go test -race -cover ./internal/features/myfeature

# 5. 静态检查
go vet ./internal/features/myfeature
gofmt -l ./internal/features/myfeature
```

---

## 问题定义要素表

在开始编码之前，必须澄清以下要素：

| 要素 | 问题 | 示例 |
|------|------|------|
| 目的 | 解决谁的什么问题？ | 解决运维人员批量管理服务器的问题 |
| 场景 | 在什么情况下触发？ | 在凌晨批量执行部署任务时 |
| 结构 | 输入→处理→输出？ | 服务器列表→并行执行→结果汇总 |
| 约束 | 性能、安全、技术边界？ | 最多 100 台，支持 SSH key 认证 |
| 验收 | 怎么算"完成"？ | 所有服务器部署成功，返回详细日志 |

**如果需求不清晰，优先向用户提出不超过 3 个澄清问题。**

---

## 模块拆分原则

将大功能拆为独立的、可验证的微模块：

```
Feature/
├── module_1/
│   ├── module_1.go       # 业务逻辑
│   └── module_1_test.go  # 单元测试
├── module_2/
│   ├── module_2.go
│   └── module_2_test.go
└── integration_test.go   # 集成测试
```

**每个模块原则**：
- 单一职责（SRP）
- 有明确的输入输出
- 可独立验证
- 最小依赖

---

## 代码生成循环

循环执行以下步骤：

```
┌─────────────────────────┐
│  1. 设计思路 + 数据结构  │
│     （等用户确认）        │
├─────────────────────────┤
│  2. 生成完整代码         │
│     （含错误处理）        │
├─────────────────────────┤
│  3. 生成单元测试          │
│     （含边界测试）        │
├─────────────────────────┤
│  4. 验证测试通过          │
│     （go test）          │
└─────────────────────────┘
         ↓
    下一个模块
```

---

## 集成验证命令

```bash
# 1. 静态检查
go vet ./...
gofmt -l .

# 2. 单元测试 + 覆盖率
go test -race -cover ./...

# 3. 集成测试（如有）
go test -tags=integration ./...

# 4. 构建验证
go build -o bin/app ./cmd/app
```
